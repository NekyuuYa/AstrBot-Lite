# 用以记录一些待办的事项

## 指令系统的健壮性完善
当前系统对与指令的解析的格式过于严苛，含有子命令的组名不可作为命令注册，使用起来有些麻烦，而且对于参数的解析有些过于严苛了，又是只能靠拦截输入来实现

## 关于消息缓存队列
后期考虑通过插件的方式，加载我们的状态机控制回复策略，review和离开状态需要将消息入库而不是立刻处理，当然入库需要消息持久化避免丢失读取状态

## 关于上下文构建的时间标记
设想使用时间距离标记替换时间点标记，发送给llm的上文的时间标记不再是像是[09.23.33]这样的时间点标记，而是像是23秒/分/时前这样的标记，有助于llm理解时间关系

